
You are a Senior Software Engineer.

I implement clean, well thought out code with proper error handling and maintainable architecture.

**Issue Title**: [PR Feedback] Document Store

**Description**:
Based on consolidated PR review, address the following in **Document Store**:

- **`get_version_diff` Returns Empty `removed_chunks` for Retired Chunks**: At `src/context_library/storage/document_store.py:655-660`, `get_version_diff` calls `get_chunk_by_hash` for removed hashes, but `get_chunk_by_hash` filters out retired chunks (`retired_at IS NULL` at line 838). Removed chunks between versions are typically retired by `retire_chunks()`, so lookups return `None` and log misleading warnings about "possible data integrity issue" (lines 662-666). The `removed_chunks` field will be systematically empty during normal operation.
- **`_build_chunk_from_row` Has No Error Handling for Corrupt Metadata**: At `src/context_library/storage/document_store.py:694-702`, `json.loads(row["domain_metadata"])` raises `json.JSONDecodeError` with no context about which chunk is corrupt, and `tuple(domain_metadata.pop("_system_cross_refs"))` raises `TypeError` if the stored value is not iterable. A single corrupt row crashes any chunk-reading operation with an inscrutable traceback.

---
_Created by PR Review Stage - Consolidation Phase_

---

## Concurrent Fix Issues (Same Review Cycle)

The following issues are being fixed simultaneously in this review cycle. Check these issues for any files they modify before making your own changes to avoid introducing conflicts:

- #172: [PR Feedback] Cross-Reference Detection
- #174: [PR Feedback] Provenance API
- #175: [PR Feedback] Vector Store & Distance Metrics
- #176: [PR Feedback] Reranker
- #177: [PR Feedback] Model Validation
- #178: [PR Feedback] Code Comments & Documentation



