services:
  context-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - sqlite-data:/data/sqlite
      - chromadb-data:/data/chromadb
    restart: unless-stopped
    environment:
      - CTX_EMBEDDING_MODEL=all-MiniLM-L6-v2
      - CTX_WEBHOOK_SECRET=${WEBHOOK_SECRET:-}
      - CTX_ENABLE_RERANKER=false

volumes:
  sqlite-data:
  chromadb-data:
